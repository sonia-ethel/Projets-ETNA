/*Créez une fonction nommée addTwoNumbers.
Cette fonction doit prendre deux paramètres de type number : a et b.
La fonction doit calculer leur addition et afficher le résultat dans le terminal.
La fonction ne retourne rien. */

function addTwoNumbers(a: number, b: number): void {
	console.log(a + b);
}
